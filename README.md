# Dash-Plotly-Bee-Demo

Simple Dash-Plotly app to overview data about bees and deseases related to colonies inside the United States

To execute locally just run inside the directory:

```
python intro-plotly.py
```